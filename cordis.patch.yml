# dsh-whale-musume bundle patch —— overlays the Umika desktop pet onto a profile composition (zero-intrusion).
# One package, two sides (dsh.bundle + dsh.client):
#   · Host plugin (worker side): serves bundled assets and proxies the local Kokoro sidecar;
#   · Browser plugin (dsh.client): injects the Umika styles and state machine script, with the asset root
#     pointing at that route; wakes the mascot automatically on first mount; all assets load locally, with no external requests and no telemetry.
- insert:
    - id: dsh-whale-musume
      name: dsh-whale-musume
